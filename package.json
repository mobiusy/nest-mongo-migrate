{
  "name": "nest-mongo-migrate",
  "version": "0.0.3",
  "description": "A nestjs module to matain mongodb incremental scripts, and run scripts to mongodb",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "*.md",
    "template/**/*"
  ],
  "readmeFilename": "README.md",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf dist/*",
    "build": "tsc",
    "prepublish": "npm run build"
  },
  "keywords": [
    "nestjs",
    "mongo"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mobiusy/nest-mongo-migrate.git"
  },
  "author": "Mobiusy <mobiuskaikai@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@nestjs/common": "^9.3.12",
    "mongodb": "^5.1.0",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "rimraf": "^4.4.1",
    "typescript": "^5.0.2"
  }
}
